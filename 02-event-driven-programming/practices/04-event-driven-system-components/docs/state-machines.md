# State Machines

The repository contains flat, table-driven and hierarchical state machine examples.

```text
OFF --START--> ACTIVE --STOP--> OFF
```

HSM events bubble from leaf state to parent until handled.

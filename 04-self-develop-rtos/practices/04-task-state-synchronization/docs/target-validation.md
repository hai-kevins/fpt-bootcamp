# Target Validation

Ghi lại board, toolchain, UART log, SysTick frequency, EXTI behavior, task state snapshot, semaphore/mutex state, priority-inheritance trace và stack guard result. Không đánh dấu PASS trước khi flash và quan sát board thật.

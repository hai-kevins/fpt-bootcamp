# Task Registry

All-task list chứa mọi task đã tạo để diagnostics, lookup và stack scanning. Registry không thay ready queues và có thể chứa task ở mọi state. Dùng `all_task_node`, không dùng `ready_node`.
